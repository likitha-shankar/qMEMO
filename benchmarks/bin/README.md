# benchmarks/bin

Generated benchmark executables.

## Notes

- This directory is populated by `make all` in `benchmarks/`.
- Binaries here are build artifacts and can be recreated at any time.
- If you need a fresh state, run `make clean` and rebuild.
