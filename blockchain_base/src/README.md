# blockchain_base/src

Baseline/control implementation sources used for side-by-side comparison with qMEMO enhancements.

## Use Case

- Build and run this path when validating baseline behavior.
- Compare with `blockchain/src/` to isolate PQC and architecture changes.

See `docs/HARSHA_BASELINE_DIFF.md` for file-level and behavior-level differences.
