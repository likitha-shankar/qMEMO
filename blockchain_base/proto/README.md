# blockchain_base/proto

Protocol Buffers schema/bindings for the baseline blockchain path.

## Notes

- Mirrors wire-format contracts used by baseline binaries.
- Keep protocol updates coordinated with baseline source and generated bindings.
