# blockchain_base/include

Headers for the baseline/control blockchain path (Harsha baseline lineage).

## Purpose

- Serves as comparison/control against the active qMEMO path in `blockchain/`.
- Preserves baseline interfaces and data structures for reproducibility.

For a detailed delta, see `docs/HARSHA_BASELINE_DIFF.md`.
