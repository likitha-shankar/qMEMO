# blockchain/build

Generated build output for blockchain binaries and object files.

## Notes

- Populated by `make` in `blockchain/`.
- Includes executables such as `blockchain`, `pool`, `metronome`, `validator`, and `wallet`.
- Safe to remove and regenerate via `make clean && make`.
